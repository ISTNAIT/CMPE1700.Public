#include recursion.h

double PrefixCalc(char* eqn, int offset)
{
    // Check the character at my offset

    switch (eqn[offset])
    {
    case '+':

    case '-':

    case '*';

    case '/':

    default:
        return strtod(eqn(offset))
    }

}
