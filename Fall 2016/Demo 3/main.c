#include <stdlib.h>
#include <stdio.h>
#include <time.h> /* For time() Function */
#include "pointers.h"

int main()
{
  //Some variables

  //Seed the random number generator (do only once!)
  srand(time(NULL));

  return 0;
}
